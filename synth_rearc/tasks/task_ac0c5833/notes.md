`arc2_opus46_summary.json` was mostly correct for this task: the real rule is add-only motif copying from the seed `2` patch to every missing corner of a `4` L-marker, using the corresponding reflection.

Official ARC-2 demonstration 3 was corrupt: its input seed contained two extra cells, so the six-cell motif in its output did not match. This inconsistency is independently called out in [`fchollet/ARC-AGI#123`](https://github.com/fchollet/ARC-AGI/issues/123) ("Demonstration 3: Red pixel patterns in input and output don't match"). The current official ARC-1 copy already has the correct six-cell input, and the local ARC-2 copy now matches it, so the verifier implements only the general rule.
